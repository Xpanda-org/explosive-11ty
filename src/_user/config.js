// src/_user/config.js
module.exports = {
  // Add Eleventy plugins
  // ex: plugins: [{ plugin: require('some-plugin'), options: {} }],
  plugins: [],

  // Add Nunjucks shortcodes
  // ex: shortcodes: { myShortcode: (arg1, arg2) => { return `...`; } },
  shortcodes: {},

  // Add Nunjucks filters
  // ex: filters: { myFilter: (value) => { return `...`; } },
  filters: {},

  // Add Passthrough Copy targets
  // ex: passthroughCopy: ['src/my-assets'],
  passthroughCopy: [],
};
